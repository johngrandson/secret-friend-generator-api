"""Code-host adapter implementations — concrete ``ICodeHost`` impls.

Add new host integrations as sibling sub-packages (e.g. ``gitlab/``,
``bitbucket/``). Each must satisfy
``src.contexts.symphony.domain.code_host.ICodeHost`` structurally.
"""
