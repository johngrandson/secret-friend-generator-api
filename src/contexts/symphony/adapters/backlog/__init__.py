"""Backlog adapter implementations — concrete ``IBacklogAdapter`` impls.

Add new tracker integrations as sibling sub-packages (e.g. ``jira/``,
``github_issues/``). Each must satisfy
``src.contexts.symphony.domain.backlog.adapter.IBacklogAdapter``
structurally.
"""
